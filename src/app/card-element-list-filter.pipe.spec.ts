import { CardElementListFilterPipe } from './card-element-list-filter.pipe';

describe('CardElementListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CardElementListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
