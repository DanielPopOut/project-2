import { ShowNoNamePipe } from './show-no-name.pipe';

describe('ShowNoNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowNoNamePipe();
    expect(pipe).toBeTruthy();
  });
});
